import { Board } from '../../features/board';
import { Layout } from '../../features/layout';

export const BoardPage = () => {
    return(
        <Layout>
            <Board />
        </Layout>
    )
}