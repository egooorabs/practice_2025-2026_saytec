import { Desk } from '../../features/desk';
import { Layout } from '../../features/layout';

export const DeskPage = () => {
    return(
        <Layout>
            <Desk />
        </Layout>
    )
}