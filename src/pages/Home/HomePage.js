import { Home } from '../../features/home';
import { Header } from '../../features/header';

export const HomePage = () => {
    return(
        <>
            <Header />
            <Home />
        </>
    )
}