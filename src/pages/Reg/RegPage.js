import { Reg } from '../../features/auth';
import { Header } from '../../features/header';

export const RegPage = () => {
    return(
        <>
            <Header />
            <Reg />
        </>
    )
}