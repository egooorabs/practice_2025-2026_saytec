import { Auth } from '../../features/auth';
import { Header } from '../../features/header';

export const AuthPage = () => {
    return(
        <>
            <Header />
            <Auth />
        </>
    )
}